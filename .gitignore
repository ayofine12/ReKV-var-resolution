# Intentionally no repository-level ignore rules.
#
# With this file empty of patterns, files under this repository are eligible
# for `git add` unless they are ignored by .git/info/exclude or a global
# core.excludesFile setting.
